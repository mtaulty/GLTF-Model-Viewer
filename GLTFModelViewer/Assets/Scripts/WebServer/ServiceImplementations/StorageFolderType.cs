namespace UwpHttpServer
{
    /// <summary>
    /// The code only implements this one folder so there's no choice just yet...
    /// </summary>
    public enum StorageFolderType
    { 
        ObjectFolder3D
    }
}
